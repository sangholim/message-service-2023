spring:
  main:
    lazy-initialization: true
  rsocket:
    server:
      transport: websocket
      port: 8080
      address: localhost
      mapping-path: /message-service

logging:
  level:
    talk:
      messageService: DEBUG
